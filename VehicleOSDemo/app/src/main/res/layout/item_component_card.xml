<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    app:cardCornerRadius="@dimen/component_card_default_border_radius"
    app:cardBackgroundColor="@color/component_card_default_fill"
    app:cardElevation="@dimen/elevation_1">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="@dimen/component_card_default_padding">

        <!-- Product Image - Asset token URL loaded programmatically -->
        <ImageView
            android:id="@+id/productImage"
            android:layout_width="match_parent"
            android:layout_height="120dp"
            android:contentDescription="Product image"
            android:layout_gravity="center_horizontal"
            android:layout_marginBottom="@dimen/spacing_md"
            android:scaleType="centerCrop"
            android:background="@color/color_surface_secondary" />

        <!-- Tires Heading - Typography heading-80: fontSize 36, lineHeight 48, fontFamily from brand -->
        <TextView
            android:id="@+id/tiresHeading"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Tires"
            android:textColor="@color/color_text_primary"
            android:textSize="@dimen/font_size_36"
            android:lineSpacingExtra="@dimen/line_height_48"
            android:layout_marginBottom="@dimen/spacing_md"
            android:fontFamily="@string/typography_heading_80_font_family"
            android:gravity="start" />

        <!-- Info Section -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:layout_marginTop="0dp">

            <!-- Bar Info Row -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:layout_marginBottom="0dp"
                android:minHeight="@dimen/line_height_32">

                <!-- Label - Using brand primary primary color -->
                <TextView
                    android:id="@+id/labelText"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="Low Tire Pressure"
                    android:textColor="@color/color_brand_primary_primary"
                    android:textSize="@dimen/font_size_24"
                    android:fontFamily="sans-serif-medium" />

                <!-- Value - Using brand primary primary color -->
                <TextView
                    android:id="@+id/valueText"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="65%"
                    android:textColor="@color/color_brand_primary_primary"
                    android:textSize="@dimen/font_size_24"
                    android:fontFamily="sans-serif-medium"
                    android:gravity="end" />
            </LinearLayout>

            <!-- Progress Bar Container -->
            <FrameLayout
                android:id="@+id/progressBarContainer"
                android:layout_width="match_parent"
                android:layout_height="@dimen/spacing_md"
                android:layout_marginTop="@dimen/spacing_xs"
                android:layout_marginBottom="@dimen/spacing_md"
                android:background="@drawable/progress_bar_background"
                android:padding="@dimen/spacing_xs">

                <!-- Progress Bar Fill -->
                <View
                    android:id="@+id/progressBarFill"
                    android:layout_width="0dp"
                    android:layout_height="match_parent"
                    android:background="@drawable/progress_bar_fill"
                    android:layout_gravity="start|center_vertical" />
            </FrameLayout>

            <!-- Replacement Due Text - Typography body-100: fontSize 20, lineHeight 32, fontFamily from brand -->
            <TextView
                android:id="@+id/replacementText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Replacement Due: #,### mi"
                android:textColor="@color/color_text_primary"
                android:textSize="@dimen/font_size_20"
                android:lineSpacingExtra="@dimen/line_height_32"
                android:fontFamily="@string/typography_body_100_font_family"
                android:layout_marginBottom="@dimen/spacing_sm" />

            <!-- Action Button - Using button component tokens -->
            <Button
                android:id="@+id/actionButton"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="View Details"
                android:textColor="@color/component_button_primary_default_text_color"
                android:background="@drawable/button_primary_background"
                android:layout_marginTop="@dimen/spacing_md"
                android:paddingTop="@dimen/component_button_primary_default_vertical_padding"
                android:paddingBottom="@dimen/component_button_primary_default_vertical_padding"
                android:paddingStart="@dimen/component_button_primary_default_horizontal_padding"
                android:paddingEnd="@dimen/component_button_primary_default_horizontal_padding"
                android:textSize="@dimen/font_size_24"
                android:fontFamily="sans-serif-medium" />
        </LinearLayout>
    </LinearLayout>
</androidx.cardview.widget.CardView>


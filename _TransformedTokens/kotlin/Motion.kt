package com.example.hmithemedemo.ui.theme

import androidx.compose.ui.unit.ms

object MotionTokens {
    // Durations (milliseconds)
    val durationshort = 100.ms
    val durationmedium = 200.ms
    val durationlong = 300.ms

    // Easing Functions
    val easingstandard = "cubic-bezier(0.4, 0.0, 0.2, 1)"
    val easingdecelerate = "cubic-bezier(0.0, 0.0, 0.2, 1)"
    val easingaccelerate = "cubic-bezier(0.4, 0.0, 1, 1)"

    // Transitions (combined duration + easing)
}
